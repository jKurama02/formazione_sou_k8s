pipeline {
    agent {
        label 'jenkins-agent_1'
    }
    stages {
        stage('Esegui Helm Install') {
            steps {
                sh '''
			helm upgrade --install flask-app /home/jenkins/workspace/formazione_sou_k8s/charts/flask-app-chart --namespace formazione-sou
                '''
            }
        }
    }
}
